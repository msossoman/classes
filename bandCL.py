class Band(object):

	def __init__(self, name, instrument):
		self.name = name
		'''self.instrument = instrument

	def getName(self):
		return self.name

	def getInstrument(self):
		return self.instrument

	def __str__(self):
		return "%s plays the %s." % (self.name, self.instrument)'''